# Skillmate Server

Environment:

```
cp .env.example .env
```

Run:

```
npm install
npm run dev
```

Routes: `/api/users`, `/api/skills`, `/api/match`, `/api/chat`, `/api/feedback`, `/api/sessions`, `/api/uploads`, `/api/admin`
